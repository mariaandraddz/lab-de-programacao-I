#include <iostream>
#include <iomanip>
using namespace std;

int main() 
{
  long int a, b, c, d, e;
    cin>>a>>b>>c>>d>>e;
    long int inimigas;
      inimigas = a - (b + c + d+ e);
      
      cout<<inimigas<<endl;
      return 0;
}
